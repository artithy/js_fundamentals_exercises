let N = 9;
for (let row = 1; row <= N; row++) {
    let line = "";

    for (let space = 1; space <= N - row; space++) {
        line = line + " ";
    }


    for (let star = 1; star <= row; star++) {
        line = line + "* ";
    }


    console.log(line);
}
