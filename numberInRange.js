let num = 25;
let start = 10;
let end = 30;

if (num >= start && num <= end) {
    console.log(num + " is within the range " + start + " to " + end);
} else {
    console.log(num + " is NOT within the range " + start + " to " + end);
}
